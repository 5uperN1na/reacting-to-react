
import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(<App name="Lover"/>, document.getElementById('root'));

 const Name = (props) => {
    return (
        "Returning something!"
     );
};
export default Name;
